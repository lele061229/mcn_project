# Python 3.10 / 3.11（推荐）使用以下版本：
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
pydantic>=2.6,<3.0
openpyxl>=3.1,<4.0
python-multipart>=0.0.9   # 文件上传（UploadFile）必需

# ─── Python 3.14 兼容（pydantic v2 无预编译 wheel）───
# 如果你的环境是 Python 3.14，请注释上面 7 行，取消注释下面 7 行：
# fastapi<0.100
# uvicorn
# httpx
# python-dotenv
# pydantic<2
# openpyxl
# python-multipart
