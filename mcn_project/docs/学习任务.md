# 学习任务清单 —— 跟着项目学全栈

> 用法：按顺序做，做完把 `[ ]` 改 `[x]`，并在《学习日志》里写一条记录。
> 卡住了直接在对话里问我，我负责讲解和复盘；做完一个任务告诉我，我来帮你 review 代码。

## 阶段 0 · 环境与全景（约 30 分钟）

- [ ] **0.1 通读《代码导读》**（docs/代码导读-20260916.md），重点记住第 2 节的通用链路模式（前端校验→fetch→服务端裁决→写库留痕→乐观更新）
- [ ] **0.2 启动本地服务**：`cd /d/Projects/mcn_project/mcn_project && node v1/server.js`，浏览器打开 http://127.0.0.1:3000
- [ ] **0.3 用 6 个 demo 账号各登录一遍**（admin / demo-promote / demo-recruit / demo-staff / demo-senior / demo-finance，密码见登录页），感受「同一个系统不同岗位看到的东西不一样」
- [ ] **0.4 跑一遍回归**：`python v1/check-crm-api.py`，看到 215 项全绿（这就是改代码前后的安全网）

## 阶段 1 · 读懂一条完整链路（约 1 小时）

- [ ] **1.1 交接链路**：对照《代码导读》第 2、3 节，从 `talent-leads.js:601 saveHandover` 一路读到 `server.js:1133 createHandover` → `server.js:1191 confirm`。用 demo-recruit 账号实际发起一次交接、再用接收账号确认
- [ ] **1.2 转正链路**：读 `server.js:1006 promoteToTalent`，理解为什么它必须是双表转化的唯一入口；用 admin 转化一条线索，观察 db.json 里 leads→talents 的变化（转之前先 `cp v1/data/db.json v1/data/db.json.bak-学习`）
- [ ] **1.3 权限链路**：读 `server.js:2042 GET /api/tasks`，回答一个问题（写进学习日志）：「为什么前端隐藏了按钮，服务端还要再判一次权限？」

## 阶段 2 · 动手改代码（每个 10~20 分钟）

- [ ] **2.1 练习一（已完成打勾）**：dueSoon 窗口 2 天→3 天 + 看板新增「任务即将逾期」提醒（步骤见《代码导读》第 6 节）
  - 状态：未开始
  - 验收：任务页徽标覆盖 3 天窗口、看板出现琥珀色提醒、check-biz-chain 28 项全绿
- [ ] **2.2 练习二**：把 2.1 里的 `2`/`3` 天提成 server.js 顶部的常量 `DUE_SOON_DAYS`，体会「业务参数集中定义」
  - 验收：只改一处常量，行为可切换；回归全绿
- [ ] **2.3 练习三**：给达人档案列表加一列「任务进行中」（后端 `GET /api/mvp/talents` 已经返回 `taskActive`，你只需要在 index.html 档案表格加一列 + 移动端卡片加一行）
  - 提示：先 grep `taskTotal` 看现有列怎么写的，照葫芦画瓢；改 app.js/talent-leads.js 记得 `?v=` 版本号 +1
  - 验收：档案页能看到新列；check-crm-ui 全绿
- [ ] **2.4 练习四**：读 `server.js` 里的 `doAssign` 和 `POST /api/mvp/leads/:id/assign-ack`（达人分配确认流，20260918e），回答：为什么「确认接收」要校验 `assignState==='pending_assign'`，只校验「达人 owner 是我」行不行？
  - 提示：想想重复点确认按钮、以及早就接收过的老达人再点一次确认这两个场景
  - 验收：答案写进学习日志；跑 check-crm-api 时能指出哪几项断言在保护这段逻辑
- [ ] **2.5 练习五**：读 `server.js` 里的 `pushMsg` / `opsLoadCandidates` 和 `GET /api/mvp/leads/auto-assign-suggest`（消息中心 + 自动分配建议，20260919a），回答：为什么自动分配只生成建议、不直接改线索的 owner？如果直接自动分配会踩哪些业务红线？
  - 提示：对照 SLA 模块的「设计红线：系统不自动换负责人」，以及高级运营对分配质量的把关职责
  - 验收：答案写进学习日志；能在 check-crm-api 里指出「自动分配只生成建议」对应的断言
- [ ] **2.6 练习六**：读 `server.js` 里 `POST /api/leads` 的报名字段映射（20260919b），回答：为什么映射字段（talentName/source/…）要和现有 name/channel 字段并存，而不是把 name 直接改名为 talentName？
  - 提示：想想存量数据、以及所有用到 name 的前端/接口代码
  - 验收：答案写进学习日志；能指出 check-crm-api 里「报名字段显式映射」断言保护了什么
- [ ] **2.4 练习四**：在交接弹窗里加一个「达人粉丝数」输入框（字段名 `fansAtHandover`），从弹窗 → POST body → server.js 落库 → 交接单回显，走通全链路
  - 这是综合考试：会同时碰到 index.html / talent-leads.js / server.js 三层
- [ ] **2.5 练习五（SLA 主题，对应 v20260916l）**：把高潜强意愿的严格 SLA 从 10/30 分钟改成 15/45 分钟（改 `server.js` 的 `SLA_STRICT` 常量），并在《代码导读》的练习思路下回答：为什么 `slaStatus` 不需要改任何代码就能生效？
  - 提示：答案在「读时派生」—— slaOf 每次读列表时现算，规则变了所有接口自动跟上
  - 验收：A 级线索的 `slaRemindMin/slaOverdueMin` 变成 15/45；`python v1/check-sla.py` 里"严格档时限 10/30"这条断言需要同步改成 15/45（体会：改业务规则要连着改回归）
  - **注（20260921a）**：SLA 已从业务前端收缩（卡片/列/筛选已移除，`slaSweep` 已 no-op），本练习保留是因为它仍是「读时派生」最好的教材，做完即懂这个模式
- [ ] **2.6 练习六**：~~给「待升级线索」筛选结果加合计数徽章~~（该筛选已随 SLA 收缩移除，此题作废）；改为：给达人线索列表新加的「审核状态」列做一个和 leadType 徽标同款的彩色徽章（pending_review=琥珀 / approved=翡翠 / supplement=玫瑰 / 空=灰）
- [ ] **2.7 练习七（看板主题，对应 v20260916n）**：看板「30 天新增线索趋势」现在的数据来自服务端 `board.trend30`。动手：把收益走势图（`trendChart`）也改成像 trend30 一样的"数据到了自动重绘"之外，再回答两个问题——① 为什么口径类统计放服务端算比前端算好？② `watch(canTasks)` 修的是什么类型的 Bug（提示：异步时序）？做完跑 `node v1/check-crm-ui.js` 应 49 项全绿

- [ ] **2.8 练习八（角色模型主题，对应 v20260916q）**：本次删寄拍岗 + 加高级运营涉及 8 个文件。动手小改：给达人档案页的「生命周期」列加颜色区分（lead=灰 / coaching=紫 / potential=amber / contracted=emerald / company_owned=靛蓝），提示：照抄 `talentStatusTd` 里现有 span 的 class 写法，或封装一个 `tl.talentStatusClass(k)` 函数
  - 验收：五种生命周期徽标颜色可区分；`node v1/check-crm-ui.js` 49 项全绿
  - 思考题：为什么 `isSupervisor` 写成一个函数而不是在每个接口里重复 `roleCode==='admin' || position==='senior_ops'`？（提示：这次改动 8 处判断只改了 1 处定义）

- [ ] **2.9 练习九（中台化主题，对应 v20260916r）**：本次把工作台改成了「服务端装配 panels、前端纯渲染」。动手小改：给管理员工作台的「达人成长漏斗」加一列绝对人数标签（现在只显示百分比），提示：`buildWorkbenchPanels` 里 funnel 的 items 已含每阶段人数，前端 funnel 渲染处补一个 `{{ it.value }} 人`
  - 验收：漏斗每条右侧同时显示转化率和人数；刷新工作台无报错
  - 思考题：为什么 ops-tasks 要独立成一张表而不塞进 tasks 表加个 type 字段？（提示：看两边的字段清单——一条是「一物一达人」的执行单，一条是「一方向多达人」的运营动作）

- [ ] **2.10 练习十（审核流主题，对应 v20260921a）**：读 `server.js` 里 `POST /api/leads` 的付费孵化自动分配分支、`submit-review` 和 `review` 两个路由，回答三个问题：① 为什么审核通过要走 `promoteToTalent()` 而不是直接往 talents 表插一条？② 自动分配 `assignedBy='system'` 和人工分配在消息/留痕上有什么差别？③ `reviewState` 为什么用空串表示「未进入审核」而不是再加一个 'draft' 状态？
  - 提示：①对照《代码导读》转化不变量小节；②对照 `doAssign` 里 lead_reassigned 新旧负责人双通知
  - 验收：答案写进学习日志；能指出 check-crm-api 12.8 节里保护这三个答案的断言

- [ ] **2.11 练习十一（验收复盘主题，对应 v20260921b）**：这次外部验收报告 3 条 P0 有 2 条是误报（测试方拿 admin 冒充 ops），但复验过程挖出了真根因——前端内置的 19 条 MOCK 演示数据。动手做两件事：① `git show` 看这次 `talent-leads.js` 的 diff，解释为什么 `const list = reactive([])` 之后运营岗页面条数会从 25 变成 10；② 在 `check-crm-api.py` 12.9 节里找到「轮询探针 count 与列表接口条数一致」这条断言，回答：如果没有这条断言，前端再混入一条假数据时回归会不会红？
  - 提示：25 = 10（API 真实返回）+ 15（MOCK 里 owner=王浩 2 条 + 未分配 13 条，`visibleList` 对公海放行）
  - 思考题：为什么「转为正式达人」收敛给主管后，`check-split-smoke.py` 必须同步反转断言？如果只改代码不改断言，会出现什么假象？
  - 验收：答案写进学习日志；能独立复述「验收报告先复现再修」的完整流程（对账号 → 对数据 → 对代码 → 再动手）

## 阶段 3 · 独立小功能（学习里程碑）

- [ ] **3.1** 自己设计并实现一个新 alert（例如「孵化费待收」）：自己定口径、改后端、加前端展示、自己写回归断言、自己 review
- [ ] **3.2** 通读《开发日志》里每条的「学习价值」部分，把没搞懂的点列出来问我

## 学习约定

1. 每完成一个任务 → 在《学习日志》记一条（几句话就行）
2. 每次我改进平台 → 我同步更新《开发日志》并检查这份清单是否需要加新任务
3. 改代码前先备份 db.json，改完必跑回归 —— 这是本项目的铁律
