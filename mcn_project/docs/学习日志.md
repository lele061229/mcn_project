# 学习日志

> 用法：每次学习/动手后在最上面追加一条，格式随意，几个要点即可。坚持记，一个月后回看会很有成就感。

---

## 2026-09-16 · 第一次：建立双线体系

**做了什么**：
- 通读《代码导读》，建立了学习任务清单和这份日志
- 了解了项目全貌：零依赖 Node 单文件后端 + Vue3 前端 + JSON 文件库，7 套回归脚本 364 项断言

**今天学到的最重要的三个概念**：
1. **派生字段读时计算**（opsPending/overdue）：不落库，每次读列表现算，数据变了它自动对，不需要定时任务
2. **权限在服务端裁决**：前端隐藏按钮只是体验优化，服务端 403 才是安全边界
3. **两阶段交接**：发起交接只挂 pending 标记，接收人 confirm 才真正改负责人

**疑问**：（暂无，做阶段 1 时往这里填）

**下一步**：阶段 0.2 启动本地服务 → 0.4 跑回归 → 开始练习一
